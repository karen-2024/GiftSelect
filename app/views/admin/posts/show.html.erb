
<div class="img1">
  <div class="img1-title">
    <h1>POST SHOW</h1>
  </div>
</div>

<div class='container'>
  <div class="row">

    <div class="contents">
      <article>
        <div class="show-title">
          <h5>投稿日：<%= @post.created_at.strftime('%Y/%m/%d') %></h5>
          <div><%= link_to '削除', admin_post_path(@post), method: :delete, data: { confirm: '『投稿』を削除しますか？' }, class: "btn-sm btn-danger" %></div>
        </div>

        <p><%= image_tag @post.get_image(700,500) %></p>

        <p>
          <h4><strong>商品名：<%= @post.name %></strong></h4>
          <h5>説明：<%= @post.introduction %></h5>
        </p>

        <p>
          数量：<%= @post.amount %>　　
          【　個包装：<%= @post.package %>　】
        </p>
        <p>価格：<%= @post.price %>円</p>
        <p>
          場所：<%= @post.prefecture %>　　　
          詳細：<%= @post.location %>
        </p>
        <p>「<%= @post.recommend %>」オススメ</P>
        <p><%= render "public/posts/static_rate", post: @post %></p>

        <p>
          <%= "★いいね： #{@post.favorites.count}" %>　　
          コメント件数： <%= @post.post_comments.count %>
        </p>

        <div class="post-review">
          <h4 class="review-title">Review</h4>
          <table width="100%">
            <tr>
              <td width="15%">
                <%= image_tag @post.user.get_profile_image(80,80), style: "border-radius: 50%; object-fit:cover;" %>
              </td>
              <td width="85%">
                <h5 class="r-title"><%= @post.user.name %></h5>
                <%= @post.review %>
              </td>
            </tr>
          </table>
        </div>

        <!--コメント-->
        <div class="comment">
          <h3 class="comment-title">Comment</h3>
        </div>

        <table class='table'>
          <% @post.post_comments.each do |post_comment| %>
            <tr>
              <td class="col-2"><%= post_comment.user.name %></td>
              <td class="col-6"><%= post_comment.comment %></td>
              <td class="col-1"><%= post_comment.created_at.strftime('%Y/%m/%d') %></td>
              <td class="col-2"><%= link_to "削除", admin_post_post_comment_path(post_comment.post, post_comment), method: :delete, data: { confirm: '『コメント』を削除しますか？' }, class: "btn-sm btn-danger" %></td>
            </tr>
          <% end %>
        </table>
      </article>
    </div>

  </div>
</div>