<div class="img1">
  <div class="img1-title">
    <h1>Search</h1>
  </div>
</div>

<div class='container'>
  <div class="row">

    <% if @model == 'user' %>
        <h3>Users search for "<%= @content %>"</h3>
        <%= render 'admin/users/index', users: @records %>
    <% else %>
        <h3>ユーザーが見つかりません。</h3>
    <% end %>

  </div>
</div>